using System;

class Program
{
    static void Main()
    {
        char[] characters = new char[] { 'a', 'b', 'c' };
        foreach (char character in characters)
        {
            Console.WriteLine(character);
        }
    }
}